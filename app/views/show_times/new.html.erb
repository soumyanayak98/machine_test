<p>create new ShowTime</p>
<%= form_for @show_time, method: :post do |f| =%>
    <%= render "form", f: f =%>
    <%= f.submit =%>
    <button>
        <%= link_to "cancel", :back =%>
    </button>
<% end %>